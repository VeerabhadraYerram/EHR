# Agent Instructions

**CRITICAL:** `EHR_Architecture_Document.md` is the absolute source of truth for the architecture of this platform. It is not to be messed with, altered, or contradicted. All design decisions, code implementation, and architectural boundaries must strictly adhere to the guidelines set in that document.

**LOGGING WORK:** All agents and developers MUST log their completed work in `progress.md`. Whenever you complete a significant chunk of work, prepend a new entry to the top of the log (below the header) so that the team can track what has been done and what remains.
